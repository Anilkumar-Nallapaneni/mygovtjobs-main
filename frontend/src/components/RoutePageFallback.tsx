import PageFallback from '@/components/PageFallback'

/** @deprecated Prefer `PageFallback` — kept as a thin alias for AppRoutes Suspense. */
export default function RoutePageFallback() {
  return <PageFallback />
}
