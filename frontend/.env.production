# Production frontend defaults — secrets via `npm run vercel:env:push:live`
# Browser origin is host-aware (govtjobs.me + livegovtjobs.com). This is prerender fallback.
VITE_SITE_URL=https://www.livegovtjobs.com
VITE_JOBS_SOURCE=supabase
VITE_DAILY_SYNC_ONLY=0
