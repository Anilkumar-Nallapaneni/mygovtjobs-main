# Update Changelog — 20 Sep 2026

## Deleted

Nothing. This update intentionally preserves the existing application and data pipelines.

## Added

- `scripts/growth-audit.mjs`
- `npm run growth:audit`
- AdSense environment documentation in `frontend/.env.example`
- `docs/GROWTH_IMPLEMENTATION_2026-09.md`
- this changelog

## Generated/refreshed during verification

- sitemap files were regenerated with the existing `npm run build:sitemap` command.
- organization index was regenerated by the existing sitemap build flow.

## Audit result from the supplied package

- snapshot jobs: 35
- live jobs: 35
- verified live jobs: 35
- published live jobs: 35
- active job sitemap URLs: 35
- archive job sitemap URLs: 0
- all 35 live rows pass the checked verification/completeness/confidence/apply-link/last-date checks

The immediate bottleneck is therefore inventory/archive/distribution scale, not basic verification quality of the 35 committed live rows.

## Validation note

`npm run growth:audit` and `npm run build:sitemap` completed successfully.

A full frontend build could not be completed in the execution window. A later type-check also showed the extracted package does not currently have the frontend npm dependencies installed in this working environment (React/Vitest/etc. modules unresolved). Run `npm install` with the repository's required Node version, then run `npm run validate` and `npm run build` locally/CI before production deployment.
